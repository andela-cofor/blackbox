<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blackboxteam.app.blackbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar">
        <activity android:name=".MainActivity" />
        <activity android:name=".LaunchScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginScreenActivity" />
        <activity android:name=".TestPage" />
        <activity android:name=".ActivateBlackboxScreenActivity" />
        <activity android:name=".DeactivaionScreenActivity" />
        <activity android:name=".ActivationScreenActivity" />
        <activity android:name=".SignUpScreen"
            android:theme="@style/SigupTheme"/>
    </application>

</manifest>